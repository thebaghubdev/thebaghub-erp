/** Inventory `status` after an authenticator returns work to the coordinator. */
export const AUTHENTICATED_RETURNED_TO_COORDINATOR_INVENTORY_STATUS =
  'Authenticated - Returned to Coordinator';

/** Inventory `status` after the coordinator sends the inquiry back to the consignor. */
export const AUTHENTICATED_RETURNED_TO_CONSIGNOR_INVENTORY_STATUS =
  'Authenticated - Returned to Consignor';

/** Inventory `status` while 3rd-party authentication fee proof awaits verification. */
export const FOR_AUTHENTICATION_PAYMENT_VERIFICATION_INVENTORY_STATUS =
  'For authentication payment verification';

/** Inventory `status` while the authenticator records paid 3rd-party results. */
export const FOR_3RD_PARTY_AUTHENTICATION_INVENTORY_STATUS =
  'For 3rd party authentication';

export const RETURNED_TO_COORDINATOR_ITEM_AUTH_STATUS =
  'Returned to Coordinator';
export const RETURNED_TO_CONSIGNOR_ITEM_AUTH_STATUS = 'Returned to Consignor';
export const FOR_AUTHENTICATION_PAYMENT_VERIFICATION_ITEM_AUTH_STATUS =
  'For authentication payment verification';
export const FOR_3RD_PARTY_AUTHENTICATION_ITEM_AUTH_STATUS =
  'For 3rd party authentication';
